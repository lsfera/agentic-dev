{
  "name": "agentic-orchestrator",
  "private": true,
  "type": "module",
  "description": "Sandcastle-based /afk orchestrator: works each ready-for-agent issue in its own disposable, git-isolated sandbox and opens a PR per issue.",
  "scripts": {
    "start": "tsx main.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test reduce.test.ts sandbox-runner.test.ts run-sh.test.ts init-sh.test.ts up-sh.test.ts",
    "test:integration": "SANDCASTLE_INTEGRATION=1 node --import tsx --test integration.test.ts"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.10.0",
    "@types/node": "^26.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
